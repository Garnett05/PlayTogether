<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="PlayTogether.Group.GroupPage"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="#2F4259" >
    <ContentPage.Content>
        <StackLayout VerticalOptions="StartAndExpand" Margin="10, 0, 10, 0" >
            <Image Source="https://cdn0.iconfinder.com/data/icons/action-states-vol-2-flat/48/Action___States_-_Vol._2-21-512.png" HeightRequest="50" HorizontalOptions="StartAndExpand" >
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding PreviousPageCommand}" />
                </Image.GestureRecognizers>
            </Image>
            <StackLayout Orientation="Horizontal" Margin="0, 10, 0, 0" >
                <Image Source="{Binding Group.image_url}" HeightRequest="70" />
                <Label Text="{Binding Group.name}" FontSize="30" TextColor="White" HorizontalOptions="EndAndExpand" VerticalOptions="Center" Margin="0, 0, 15, 0" />
            </StackLayout>
            <Frame BackgroundColor="#12A3A0" HeightRequest="335" CornerRadius="30" Margin="0, 25, 0, 0" Padding="0" >
                <StackLayout>
                    <Label Text="{Binding GroupParticipants}" Margin="20, 5, 0, 0" FontSize="14" TextColor="Black" />
                    <CollectionView
                        ItemsLayout="VerticalGrid, 3"
                        ItemsSource="{Binding GroupUsers}"
                        SelectionMode="None" >
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout Margin="25, 4, 25, 4" >
                                    <Image Source="{Binding user_image}" HeightRequest="40" />
                                    <Label Text="{Binding nickname}" TextColor="Black" HorizontalOptions="Center" FontSize="14" />
                                </StackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>                    
                </StackLayout>
            </Frame>
            <Button Text="Entrar" Margin="50, 10, 50, 0" Style="{StaticResource btn1}" Command="{Binding JoinGroupCommand}" IsVisible="{Binding EntrarButton}" />
            <Button Text="Excluir" Margin="50, 10, 50, 0" Style="{StaticResource btn1}" Command="{Binding DeleteGroupCommand}" IsVisible="{Binding DeleteButton}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>